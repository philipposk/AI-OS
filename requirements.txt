# Core
requests>=2.32,<3
python-dotenv>=1.0,<2

# Providers
anthropic>=0.40,<1
# openrouter, nvidia, groq, ollama all use plain HTTP via `requests`.

# Orchestration (wired in Phase C)
langgraph>=0.2.50,<0.5
# Persistent workflow state (opt-in via $LANGGRAPH_CHECKPOINT_DB)
langgraph-checkpoint-sqlite>=2.0,<4

# UI
streamlit>=1.36,<2

# OpenAI-compat shim (Phase O)
fastapi>=0.110,<1
uvicorn[standard]>=0.30,<1

# Storage (wired in Phase E)
chromadb>=0.5,<0.6   # optional; can be swapped for sqlite-fts5 on small VMs

# Communication
slack-sdk>=3.27,<4
# slack-bolt and python-telegram-bot are optional — install only if you run those bots.

# Per-checkpoint media (Phase Q): screenshot card + voice note
pillow>=10,<12
edge-tts>=6.1,<8     # optional; voice falls back to silent if missing

# Phase Z: self-improvement
# litellm = unified-provider client + LITELLM_PRIMARY=1 default
litellm>=1.50,<2
# dspy-ai = prompt tuning from retrospectives (`cli.py tune run` / `cli.py tune auto`)
dspy-ai>=2.5,<4
# json_repair = tolerate malformed LLM JSON in _extract_json_array
json-repair>=0.30,<1
# instructor + pydantic = USE_STRUCTURED_OUTPUTS=1 typed plan/review
instructor>=1.5,<2
pydantic>=2.5,<3

# Test
pytest>=8.0,<9
pytest-vcr>=1.0,<2
vcrpy>=6.0,<7
httpx>=0.27,<1   # FastAPI TestClient
